version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      # - command
      # - command
  #post_build:
    #commands:
        - sudo apt-get install git -y
        - wget -qO- https://cli-assets.heroku.com/install-ubuntu.sh | sh
        - git init
        - git add .
        - git config --global user.email "premnamb@amazon.com"
        - git config --global user.name "PremNambi"
        - git commit -m "Ready for deployment"
        - heroku git:remote -a still-beyond-24839
        - git push heroku master
        - echo "All success"
